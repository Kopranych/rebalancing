apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "rebalance.fullname" . }}-test-connection"
  labels:
    {{- include "rebalance.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "rebalance.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
