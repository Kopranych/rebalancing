apiVersion: v1
kind: Service
metadata:
  name: {{ include "rebalance.fullname" . }}
  labels:
    {{- include "rebalance.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "rebalance.selectorLabels" . | nindent 4 }}
